# Dashboard — Current State

> The full project snapshot. **Read this first every session.** Factual, not aspirational — it records what IS.
> Last updated: **2026-06-28** (end of Phase **B.07**).

## One-line status

The portal now has a **working, branded, config-driven editor — including featured-image upload — on top of the secure per-tenant bridge**: a signed-in user resolves server-side to the **one** client they own, and can **create, edit, save-as-draft, publish, delete, and attach a featured image to** a blog post (headline, body, summary, slug, image) on **that client's own Sanity project** — every write AND every image upload built from the session-resolved tenant's project + **decrypted** token and nothing else. The image is uploaded server-side through the same per-tenant client (never a browser token), validated (4 MB cap + JPG/PNG/WebP/GIF allowlist), and written onto the post as an asset reference (tri-state: preserve / write / clear); an existing image is surfaced in the edit form. Draft/published follows **Sanity's draft-id model** (save → `drafts.<id>`; publish → atomic promote-and-delete transaction; delete → both variants), consistent with the B.04 read reduction. The load-bearing **cross-tenant isolation test now also covers the image upload + image write** (the §5 gate): user A's create/save/publish/delete **and image upload** are dispatched only through A's project + token, and an unmapped/unauthenticated session is denied before any writer/uploader is built or any secret is read. Tokens stay **server-only** end to end (no `'use client'` module imports a token/resolver/mutation/Sanity module; the build's `server-only` guard holds; verified). **Live uploads/writes against a real Sanity project are M.02** — the B.03 placeholder project surfaces the friendly error state by design; the offline upload/write-isolation + unit tests are the authoritative B.06 proof. **B.07** settled how a publish reaches the live site: the refresh is **driven by each client's own Sanity → site webhook**, so the portal **sends nothing on publish and holds no revalidation secret** (`revalidateUrl` is retained-but-unused) — a docs-and-decision phase that adds no portal feature code and keeps the suite green; the per-client webhook setup is **M.01** and the live end-to-end proof is **M.02**.

## Links

- **GitHub:** https://github.com/DinovLazar/dashboard (public, `main` protected)
- **Vercel preview:** https://dashboard-six-iota-33.vercel.app (renders the branded shell; `/` → `/login`)
- **Vercel project:** `dinovlazars-projects/dashboard`

## What is live

**From B.01 (foundation + look):** public protected repo to the Vertex standard; Next 16 + React 19 + TS + Tailwind v4 + shadcn (`base-nova`) app that builds clean and deploys to Vercel; Vertex brand applied (dark-only); `src/_project-state/` as the docs home.

**From B.02 (Supabase auth — the locked door):** server-side Supabase auth (`@supabase/ssr` + `@supabase/supabase-js`); `src/proxy.ts` session refresh + redirect; functional `/login` (email + password Server Action, no signup, generic error); `(portal)` gated by a `getClaims()` server check; sign-out; no-cache hardening.

**From B.03 (registry data model — the secure address book):** three RLS-protected Supabase tables (`clients`, `client_users`, `client_secrets`) so a signed-in user can read only their own client + config and **cannot read any token**; app-layer **AES-256-GCM** token encryption (`src/lib/crypto/tokens.ts`); the service-role client (`src/lib/supabase/admin.ts`) as the only reader of `client_secrets`; seed/verify scripts + an operator runbook.

**New in B.04 (secure per-tenant Sanity bridge — read path):**
- **The tenant resolver** (`src/lib/registry/resolve-tenant.ts`, `server-only`): turns the logged-in user → `{ config, token }` for their one client. Derives the user id from the validated `getClaims()` session (`sub`); reads the user→client mapping and the client config through the **RLS-scoped** Supabase client; reads the encrypted token **only** through the **service-role** client, keyed on the already-resolved, user-owned `client_id`; decrypts with `decryptToken`. Fails closed with a typed `TenantResolutionError` (`unauthenticated` / `no-client` / `config-missing` / `secret-missing`). **No caller can supply a client/project id or token.** The pure core `resolveTenantWith(deps)` takes injectable seams; the production `resolveTenant` is `cache()`-deduplicated per request. Types in `src/lib/registry/types.ts` (`TenantConfig`, `TenantContext`).
- **The per-tenant Sanity client factory** (`src/lib/sanity/client.ts`, `server-only`): `createTenantSanityClient(config, token)` builds a fresh `@sanity/client` per request — `projectId`/`dataset`/`token`, `apiVersion: SANITY_API_VERSION` (`'2026-03-01'`), `useCdn: false`, `perspective: 'raw'` (so the editor sees drafts + published side by side). Throws on an empty token.
- **The read path** (`src/lib/sanity/posts.ts`, `server-only`): `listPosts(tenant, makeClient?)` fetches with the field-map query, reduces the `raw`-perspective variants to one `PostSummary` per logical post (status ∈ {draft, published}, `hasUnpublishedEdits`, draft-preferred display values, `versions.*` ignored), sorted newest-first. `makeClient` is injectable for the offline test. `displayValue` handles plain-string now / localized-object later. The token is never returned or logged.
- **The field-map → GROQ helper** (`src/lib/config/field-map.ts`): `assertSafeFieldPath` (regex guard against GROQ injection from a malformed registry row) + `buildPostListQuery` (binds `$type` as a **parameter**, interpolates only validated field names).
- **The wired `/posts` page** (`src/app/(portal)/posts/page.tsx`, Server Component): resolves the tenant, lists posts, and renders the client label + a populated list, or the empty / not-linked / not-ready / read-error states — never leaking the project id, token, or a raw error. Both "New post" buttons stay disabled (editor = B.05). The presentational list is `src/components/portal/posts-list.tsx` (gets only `PostSummary[]`, never the tenant/token).
- **Top-bar label** (`src/app/(portal)/layout.tsx`): now the resolved client label (best-effort; falls back to the email if resolution fails), sharing the page's `cache()`d resolution — no extra query.
- **The automated cross-tenant isolation test** (`src/lib/registry/isolation.test.ts`, offline): **read path** — A→A / B→B; the Sanity client is built with the session owner's projectId + token (never the other tenant's); no caller can override the client/project; the secret read is keyed to the owner's client id; the read path is unreachable for an unmapped (`no-client`) / null (`unauthenticated`) session — plus `config-missing` / `secret-missing` / decrypt-failure fail-closed paths. **Write path (B.05, the §5 gate)** — a recording writer seam proves every `createDraft`/`saveDraft`/`publishPost`/`deletePost` is BUILT with and DISPATCHED through the owner's project + token (never the other tenant's); an attacker-controlled/foreign post id still resolves only within the owner's dataset; an unmapped/null session is denied before any writer is constructed or any secret read; and the write functions expose no client/project/token selector (session identity is the sole input). Complemented by `posts.test.ts` (read reduce + `getPost`), `mutations.test.ts` (dispatch shapes), `field-map.test.ts` (injection guard + write-key validation), `localize.test.ts`, `portable-text.test.ts`, `doc-id.test.ts`. **105 tests pass.**

**New in B.05 (the config-driven editor — write path):**
- **The mutation module** (`src/lib/sanity/mutations.ts`, `server-only`): the **only** write site. `createDraft` / `saveDraft` / `publishPost` / `deletePost`, each taking a session-resolved `TenantContext` + an injectable writer factory (default = the real per-tenant client). Draft-id model: save = `createOrReplace('drafts.'+id)`; publish = one transaction (`createOrReplace` published from the draft's full content + `delete` the draft); delete = one transaction (both variants). Save does a **read-modify-write overlay** so the client's non-essential fields (image, author, categories, FAQs) are preserved. The token is never returned or logged.
- **The mutating Server Actions** (`src/app/(portal)/posts/actions.ts`, `'use server'`): `createPostAction` / `saveDraftAction` / `publishPostAction` / `deletePostAction`. Each **re-resolves the tenant itself** (Server Actions are reachable by direct POST), parses per-locale fields from `FormData`, dispatches the matching mutation, `revalidatePath`s, and returns a generic non-leaking result or `redirect`s. No tenant is passed from the page; no caller-supplied id/project/token is trusted (ids normalized).
- **The single-post read** (`src/lib/sanity/posts.ts` → `getPost` + `PostDetail`): loads one post for the edit form (draft-preferred reduce, per-locale field values, `bodyEditable` flag), id normalized; token stays server-side.
- **Pure helpers** (`src/lib/config/localize.ts`, `portable-text.ts`; `src/lib/sanity/doc-id.ts`, `src/lib/config/field-map.ts` extensions): localized value ⇄ per-locale form state; plain text ⇄ minimal Portable Text with a rich-body data-loss guard; id sanitization; the single-post query + write-key/slug-container validation.
- **The editor UI** (`src/components/editor/post-editor.tsx`, `'use client'`): branded, mobile-first form — per-locale headline/summary/body + slug, **locale tabs when multi-locale**, the featured-image control (B.06), Save-draft / Publish, and Delete with a confirm step. Receives only non-secret serializable props (locales + per-locale values + status + the image asset id/URL) — never the tenant/token. Plus the `/posts/new` and `/posts/[id]` pages and the shared `editor-message.tsx` friendly states; the `/posts` "New post" buttons now link to `/posts/new` and each list row links to `/posts/[id]`.

**New in B.06 (featured-image upload — through the same locked path):**
- **The server-only upload module** (`src/lib/sanity/assets.ts`, `server-only`): `uploadImage(tenant, file, makeUploader?)` validates BEFORE any byte is sent — rejects empty/zero-byte (`empty`), > 4 MB (`too-large`), and any type outside JPG/PNG/WebP/GIF (`unsupported-type`) via a typed `ImageUploadError` — converts the web `File` to a Node `Buffer`, and uploads through the **per-tenant** client built from the resolved tenant's own project + token (injectable `MakeUploader`/`AssetUploader` seam, mirroring `MakeWriter`). Returns `{ assetId, url }`; the token is never returned or logged. Also exports `isValidAssetId` (canonical `image-<hash>-<w>x<h>-<ext>`, 200-char bound) so a submitted asset id is checked before it is trusted as a `_ref`.
- **The image Server Action** (`src/app/(portal)/posts/actions.ts` → `uploadImageAction`): re-resolves + re-authorizes the tenant with `resolveTenant()` on every POST (never trusts a page), uploads via the module, and returns a serializable `ImageUploadState` (`{ ok, assetId, url, error }`). Oversize/unsupported/empty get a friendly, specific message; anything else gets the generic error. The token, project id, and raw error never reach the result.
- **The image write** (`src/lib/sanity/mutations.ts` → `EditorFields.image` + `applyImageIntent`): tri-state, mirroring how `body` omission preserves rich content — omitted → preserve the stored image (keeps client-side hotspot/crop/alt metadata); non-empty `assetId` → write `{ _type:'image', asset:{ _type:'reference', _ref } }`; null/empty → **actively delete** the image field (because `createOrReplace` replaces the whole doc and the read-modify-write base carries the old image). Applies to `createDraft` + `saveDraft`; `publishPost` needs no change (it promotes the draft, which already carries the image). `parseFields` derives the tri-state from the form's hidden `image` (current intent) vs `imageOriginal` (loaded value); a malformed submitted id fails generically.
- **The image read** (`src/lib/config/field-map.ts` → `buildPostByIdQuery` projects `"imageAssetId": <image>.asset._ref` + `"imageUrl": <image>.asset->url`; `src/lib/sanity/posts.ts` → `PostDetail.image = { assetId, url }`): the edit form shows an already-attached image. The post **list** is unchanged (no image). The image field name is GROQ-guarded (`assertSafeFieldPath`) in both the read projection and as a mutation key (`assertWritableFieldPaths`).
- **The editor's featured-image control** (`post-editor.tsx` → `FeaturedImageField`): pick → upload (own `useTransition`, never with Save/Publish — the file input is unnamed so its bytes are never serialized into the editor form) → `next/image` preview from the `cdn.sanity.io` URL → Remove/Replace. Two hidden inputs (`image`, `imageOriginal`) carry the intent into Save/Publish. Full a11y: a real `<Label>` for the file input, `role`/`aria-live` status + errors, everything disabled while any submit/upload is busy, clear "No image yet" empty state and friendly inline error.
- **`next.config.ts` (B.06 — the first change since B.01):** `images.remotePatterns` allows `https://cdn.sanity.io` for `next/image`; `experimental.serverActions.bodySizeLimit: '5mb'` raises the Server Action body cap above the 4 MB app cap (the Vercel 4.5 MB function-body ceiling remains the hard physical limit). Both confirmed against this Next version's in-repo docs.
- **The cross-tenant isolation test, extended again** (`src/lib/registry/isolation.test.ts`): a recording **uploader** seam proves user A's `uploadImage` is built with + dispatched through A's project + token only (never B's), takes no caller-supplied project/client/token, and is denied (before any uploader is built or secret read) for an unmapped/null session; plus the image `_ref` is written only through A's per-tenant writer. **130 tests pass** (105 → 130: new `assets.test.ts`; extended `mutations.test.ts`, `field-map.test.ts`, `posts.test.ts`, `isolation.test.ts`).

**New in B.07 (publish → live-site refresh — Sanity-webhook model, no portal call):**
- **The architecture is settled and recorded, not coded.** On publish, the client's live website is refreshed by the **client's own Sanity → site webhook**, not by an outbound call from the portal. The portal's only publish-time job is what it already does (B.05): write the published document into the client's Sanity project (promote the draft, delete it, in one transaction). Sanity then fires the client site's existing `POST <client-site>/api/revalidate` webhook and the site rebuilds the affected pages within ~1–2 minutes. **The portal makes no HTTP request to any client endpoint and holds no revalidation secret** — the decisive reason for this shape: one fewer secret per client in the app that holds editing keys to every client's blog. Recorded as the **2026-06-28** entry in `dashboard-Decisions.md`.
- **No new portal code.** The publish path (`publishPostAction` → `publishPost`) and the editor's existing success copy — `"Published — your live site will update shortly."` — were already correct for this model (baked in at B.05) and are unchanged. This phase added **no runtime code, no dependency, no env var, and no new test**; the existing **130 offline tests stay green** (the count is unchanged because nothing runtime changed — stated in the completion report).
- **`revalidateUrl` is retained-but-unused.** `TenantConfig.revalidateUrl` (from the `clients.revalidate_url` column) is now documented as **intentionally unused by the portal** in `src/lib/registry/types.ts`; the field and column are kept (not removed) so a later switch to a portal-initiated call needs no schema change. The applied SQL migration is untouched.
- **The runbook is the operator's authority.** `docs/runbooks/live-site-revalidation.md` documents how the refresh works, each client site's precondition (`/api/revalidate` receiver + its own `SANITY_REVALIDATE_SECRET`), the once-per-client manage.sanity.io webhook setup (including the load-bearing `!(_id in path("drafts.**"))` draft-excluding GROQ filter and the `{_type,_id,"slug":slug.current}` projection), the verification steps, and a per-client status table. **No secret value appears in it** — it says only *where* each secret lives.
- **Spec pointers synced.** `dashboard-Project-Instructions.md` §3 and `dashboard-Plan.md` §8 (which described the old "portal pings `revalidate_url`" assumption) each carry a one-line pointer to the 2026-06-28 decision + runbook; the original prose is intact.

## What is NOT built yet (by design)

- **No portal-side live-site revalidation call (by design, settled in B.07).** The portal deliberately does **not** call any client's `revalidate_url` and holds no revalidation secret; the live-site refresh is delegated to each client's own Sanity → site webhook (see "New in B.07" above and the 2026-06-28 Decisions entry). What is **not done yet** is the per-client *enablement*: configuring each client's Sanity publish webhook (manage.sanity.io) is **M.01**, and the live publish → live-site round-trip proof is **M.02**. Until a client's webhook is switched on, a portal publish still succeeds but that client's live site won't refresh.
- **No image cropping / hotspot / focal-point UI, alt-text editing, transformations, or per-locale / multiple images.** v1 is one featured image, non-localized (an unchanged image's existing hotspot/crop/alt metadata is preserved, never edited). Larger-than-4 MB / browser-direct uploads are explicitly deferred (see the 2026-06-27 Decisions entry).
- **No real client, real Sanity project, or real token.** B.06 still uses only the B.03 throwaway test client (placeholder project + dummy token), so a *live* read, write, or image upload errors by design (the friendly error/read-error state). Real Editor tokens are onboarded in **M.01**; the live end-to-end create → upload image → publish → delete per real client is **M.02**.
- **No new environment variables and no new dependencies.** The Sanity project id + token come from the registry, never from env. (B.06 *did* make the first `next.config.ts` change since B.01 — image `remotePatterns` + the Server Action body-size limit — but added no deps and no env vars.)
- **No full-fidelity Portable Text editing.** The editor edits the body as plain paragraphs; a body richer than simple paragraphs (marks, links, headings, lists, embeds) is shown **read-only** with an "edit in Sanity" note so it is never silently stripped. Full rich-body editing is out of scope.
- **No `status`-field writes.** Publish state is the draft-id model only; the schema `status` field is left in config but never written (per-client `status` handling, if needed, is M.01).
- **No multi-client operators, no password reset / key rotation / rate limiting, no public signup** — as before.

## Security posture (B.07 — write + image-upload path; publish refresh delegated to Sanity)

- **The portal sends nothing on publish and holds no revalidation secret (B.07).** Live-site refresh is delegated to each client's own Sanity → site webhook, so the portal makes **no outbound HTTP call to any client endpoint** and stores **no `SANITY_REVALIDATE_SECRET`** — that secret lives only on each client site (and in that client's Sanity webhook config), never in the portal or this repo. This **shrinks** the secret surface of the one app that holds editing keys to every client's blog (one fewer secret per client). The §5 write/upload isolation gate is unaffected because no new outbound call was added. See the 2026-06-28 Decisions entry and `docs/runbooks/live-site-revalidation.md`.
- **The decrypted token is a server-only secret, end to end.** It exists only inside `resolve-tenant.ts` / `sanity/*.ts` on the server. Every `lib/registry` and `lib/sanity` module — including the new `sanity/assets.ts` — starts with `import 'server-only'`; `field-map.ts` holds no secret and imports `TenantConfig` via `import type`. **Verified:** no `'use client'` file imports any token/resolver/mutation/Sanity module (the editor reaches the upload only through the `'use server'` `uploadImageAction`); `assets.ts` is imported only by `actions.ts` + the test; `npm run build` keeps the genuine `server-only` guard in force.
- **Image upload goes through the same locked per-tenant path — never a browser token.** `uploadImageAction` re-resolves + re-authorizes the tenant on every POST and uploads via the server-side per-tenant client built from the session owner's project + token; the bytes land in that one client's dataset and only the asset reference is written. A browser-direct upload (which would need a browser-exposed token) is explicitly out of scope (2026-06-27 Decisions entry). The upload is size- + type-validated server-side; a submitted asset id is shape-validated before it is trusted as a `_ref`; the token/project id/raw error never reach the `ImageUploadState` returned to the browser. The isolation test's §5 gate now covers the upload + the image write.
- **Authorize on every access AND every mutation, from the session.** The user's `client_id` is derived from the validated `getClaims()` session via the RLS-scoped client; **a caller may never supply a project id, client id, or token.** Every Server Action **re-resolves the tenant itself** (Server Actions are independently reachable by direct POST; a page/layout check does not protect them), so authorization is re-checked on every write. There is exactly one client a request may touch — the session owner's. Proven by the offline isolation test, now extended to all four mutations.
- **Caller-supplied post ids are sanitized, never trusted as selectors.** A submitted id is normalized (`drafts.`/`versions.` prefixes stripped; non-plain ids rejected) and only ever applied through the owner's per-tenant client — so an attacker-shaped id can at most touch a doc in the attacker's own dataset, never another tenant's project.
- **Defense in depth:** config reads go through the RLS-scoped client; the encrypted token is read only by the service-role client, keyed on the already-resolved `client_id`; the token is decrypted only after the user-scoped lookups succeed, and a tamper/wrong-key error propagates (never a plaintext fallback).
- **Fail closed, quietly.** Every resolver failure throws a typed `TenantResolutionError`; pages and actions turn those into friendly/generic states. The raw error, project id, token, and doc type are never rendered or returned.
- **GROQ injection guard.** `$type`/`$id`/`$draftId` are always bound parameters; all interpolated field names — read projections (incl. the B.06 image projection) AND mutation object keys (incl. the slug container and the image field) — are validated by `assertSafeFieldPath` / `assertWritableFieldPaths` before use.
- **Data-loss guard.** Save does a read-modify-write overlay (non-essential client fields preserved); a body too rich for plain-text editing is detected and shown read-only so it is never silently stripped; an **unchanged image is preserved untouched** (keeping any hotspot/crop/alt metadata) and is dropped only on an explicit clear — never silently overwritten.
- **No secret values anywhere in the diff** (code, comments, tests, report) — grep-verified; no `'use client'` file imports a token/resolver/mutation/Sanity module — the build's `server-only` guard holds. The isolation/mutation/upload tests use only invented, meaningless strings.

## Stack (see `00_stack-and-config.md` for exact pins)

Next 16.2.3 · React 19.2.4 · TS ^5 · Tailwind v4 · shadcn `base-nova` · @supabase/ssr 0.12.0 + @supabase/supabase-js 2.108.2 · server-only 0.0.1 · **@sanity/client 7.22.1 (exact)** · token encryption via Node `node:crypto` (AES-256-GCM). Dev: vitest 4.1.9, tsx 4.22.4. Hosted on Vercel. **B.06 added no new dependencies and no new environment variables** (only a `next.config.ts` change: image `remotePatterns` + `serverActions.bodySizeLimit`). **B.07 added no dependencies, no environment variables, and no runtime code** — it is docs + a decision + a TS doc-comment only (notably *not* `@sanity/webhook` or any HTTP/signing library, by design).

## Routes

`/` → `/login`; `/login` functional; `(portal)` layout gate; `proxy.ts` session refresh. Dynamic (`ƒ`): `/posts` (per-tenant read), and **new in B.05** `/posts/new` (create-mode editor) and `/posts/[id]` (edit-mode editor). Mutations **and the B.06 image upload** are Server Actions in `posts/actions.ts` (not `/api/*` route handlers — none needed). B.06 added no new routes; **B.07 added no routes** (the live-site refresh is a Sanity webhook on the *client site*, not a portal route).

## Risks & follow-ups

- **Branch/merge order:** B.02 (#2), B.03 (#3), B.04, B.05, and B.06 PRs are still open, not merged to `main`. B.07 continues the stack on its own branch, based on the head of `b06-image-upload`. **Confirm the PR base when opening the B.07 PR** (base on the open B.06 branch, not `main`, unless B.06 has merged by then — same merge-order caveat every prior B report raised).
- **The live editor render + write + image upload is operator-pending** (no `.env.local` / Supabase project is reachable by the agent). With the test client pointed at a placeholder project, a signed-in visit renders the shell/friendly states; a real create → upload image → publish → delete round-trip is the **M.02** end-to-end. The **offline upload/write-isolation + unit tests are the authoritative B.06 proof** (per the brief's "Note on live verification" and DoD).
- **Live-site refresh is now operator-enablement, not portal work (B.07 settled this).** The portal sends nothing on publish and holds no revalidation secret; what remains is **per-client** setup — configure each client's Sanity publish webhook (manage.sanity.io) and confirm each client site exposes a `/api/revalidate` receiver with its own `SANITY_REVALIDATE_SECRET`. That is **M.01**, per `docs/runbooks/live-site-revalidation.md`; the live publish → live-site round-trip proof is **M.02**. Per the status table, Sunset already has the receiver + secret; Vertex and Dalibor are to confirm.
- **Pre-existing `npm audit` advisories on `next@16.2.3`** (+ a transitive `postcss`) — **not** introduced by B.06 (zero new deps). A `next` 16.2.x bump still belongs in a dedicated maintenance PR.
- **`experimental.serverActions` is now enabled** in `next.config.ts` (for `bodySizeLimit`) — the build prints an "Experiments (use with caution): serverActions" notice. This is expected and only configures the existing (stable-since-14) Server Actions feature's body-size cap; it is not opting into an unstable feature.
- **AI review** (CodeRabbit/Codex) still an owner one-time step. Strongly recommended before this write-path phase merges.

## Next phase

**B.08 — Accessibility & polish:** keyboard/focus order, labels, error/empty/loading states, and a mobile-first responsive pass across the portal. *Done when:* the portal is keyboard-navigable, has no obvious a11y violations, and reads cleanly on a phone. See `dashboard-Phase-Plan.md`.

> B.07 (publish → live-site refresh) is closed: the refresh is delegated to each client's Sanity → site webhook, so the portal ships no new code for it — only the recorded decision, the `docs/runbooks/live-site-revalidation.md` runbook, and the retained-but-unused `revalidateUrl`. Per-client webhook enablement is **M.01**; the live round-trip proof is **M.02**.
